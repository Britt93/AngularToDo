//show multiple items (interface, service, component interaction)
export interface Item {
    id: number;
    description: string;
    listId: number;
    order: number;
    isDone: boolean;
    date: string;
}