//show multiple items (interface, service, component interaction)
export interface Item {
    id: number;
    text: string;
    listId: number;
    order: number;
    statusId: number;
    date: string;
}